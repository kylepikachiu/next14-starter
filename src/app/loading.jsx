import React from 'react'
//* Loading page is a special page that displays while the other content is still loading in
const Loading = () => {
  return (
    <div>
      Loading
    </div>
  )
}

export default Loading
